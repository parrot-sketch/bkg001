/**
 * Shared Kernel — Identity Types
 *
 * Placeholder for identity/value-object primitives shared across bounded contexts.
 *
 * **Planned:** PatientId, AppointmentId, ConsultationId, DoctorId, etc.
 * **Phase:** P1-003+
 * **Status:** Structure only — no logic yet.
 */

export type IdentityType = string;
