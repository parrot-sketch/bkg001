/**
 * Shared Kernel — Temporal Types
 *
 * Placeholder for temporal primitives (DateRange, TimeSlot, Duration, etc.)
 * shared across bounded contexts.
 *
 * **Planned:** DateRange, TimeSlot, Duration, business-hours helpers.
 * **Phase:** P1-003+
 * **Status:** Structure only — no logic yet.
 */

export type TemporalType = Date;
