# ============================================================================
# ENVIRONMENT VARIABLES
# Healthcare Management System - Production Configuration
# ============================================================================
# 
# Copy this file to .env and fill in your actual values
# For production, set these in Vercel Environment Variables
#
# cp .env.example .env
# ============================================================================

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# Aiven PostgreSQL Database (Production)
# Get this from Aiven dashboard → Service URI (click "Reveal Password")
DATABASE_URL="postgres://avnadmin:YOUR_AIVEN_PASSWORD@pg-25182a71-mbuguamuiruri12-d78e.k.aivencloud.com:22630/defaultdb?sslmode=require"

# Local Development Database (Optional - for local testing)
# DATABASE_URL="postgresql://postgres:postgres@localhost:5433/nairobi_sculpt?schema=public"

# ============================================================================
# JWT AUTHENTICATION
# ============================================================================

# JWT Secret Key (REQUIRED)
# Generate a strong random string (minimum 32 characters)
# For production: Use a cryptographically secure random generator
# Example: openssl rand -base64 32
JWT_SECRET="1zgqbDD_P=Z[x!?%[{pPXm7[{y^Ma>,j"

# JWT Refresh Token Secret (REQUIRED)
# Must be different from JWT_SECRET
# Generate a strong random string (minimum 32 characters)
# Example: openssl rand -base64 32
JWT_REFRESH_SECRET="oPzOY+n(e2DQ.<9@U0Sb*bp}$]GHl@JQ"

# JWT Token Expiration (Optional - defaults shown)
# JWT_EXPIRES_IN="15m"          # Access token expiration (15 minutes)
# JWT_REFRESH_EXPIRES_IN="7d"  # Refresh token expiration (7 days)

# ============================================================================
# NEXT.JS APPLICATION
# ============================================================================

# Application URL (REQUIRED for production)
# Your Vercel deployment URL or custom domain
# Example: https://your-app.vercel.app
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# API Base URL (Optional - defaults to /api)
# NEXT_PUBLIC_API_URL="http://localhost:3000/api"

# Node Environment
NODE_ENV="development"
# NODE_ENV="production"

# ============================================================================
# CLOUDINARY IMAGE HOSTING (Optional)
# ============================================================================

# Cloudinary Configuration
# Get these from Cloudinary Dashboard → Settings → Product Environment Credentials
# Only needed if uploading images programmatically
# If uploading via dashboard, you can skip these

# CLOUDINARY_CLOUD_NAME="your_cloud_name"
# CLOUDINARY_API_KEY="your_api_key"
# CLOUDINARY_API_SECRET="your_api_secret"

# ============================================================================
# EMAIL / NOTIFICATIONS (Optional - for future implementation)
# ============================================================================

# SMTP Configuration for email notifications
# SMTP_HOST="smtp.gmail.com"
# SMTP_PORT="587"
# SMTP_USER="your-email@gmail.com"
# SMTP_PASSWORD="your-app-password"
# SMTP_FROM="noreply@nairobisculpt.com"

# ============================================================================
# DOCKER DATABASE (Optional - for local development)
# ============================================================================

# Only needed if using Docker Compose for local database
# DB_USER="postgres"
# DB_PASSWORD="postgres"
# DB_NAME="nairobi_sculpt"
# DB_PORT="5433"

# ============================================================================
# SECURITY NOTES
# ============================================================================
#
# ⚠️  NEVER commit .env file to git (already in .gitignore)
# ⚠️  Use strong, unique secrets in production
# ⚠️  Rotate secrets regularly
# ⚠️  Use different secrets for development and production
# ⚠️  Store production secrets in Vercel Environment Variables
#
# ============================================================================
